{
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.86.0"
  },
  "scripts": {
    "sass": "sass --load-path=node_modules sass/main.scss css/style.css",
    "watch:sass": "sass --watch --load-path=node_modules sass/main.scss css/style.css",
    "start": "live-server --watch=css,js,html"
  }
}
